/**
 * core/market-data — public API. Implementation arrives per docs/ROADMAP.md.
 * Other modules may import ONLY from this file (deep imports are lint-blocked).
 */
export const MODULE_market_data_PLACEHOLDER = true;
