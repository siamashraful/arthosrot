/**
 * infra/email — adapter implementing core ports. Implementation arrives per docs/ROADMAP.md.
 * Vendor types must never be exported from this directory boundary.
 */
export const INFRA_email_PLACEHOLDER = true;
