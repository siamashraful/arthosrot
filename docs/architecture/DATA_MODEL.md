# Data model

> **Purpose:** the single reference for tables, types, precision, and constraints. Mirrors `src/infra/db/schema.ts`; when they disagree, fix one in the same PR.
> **Audience:** anyone touching the database. **Belongs here:** schema detail. **Lives elsewhere:** why (adr/), behavior (EXECUTION.md).

## Conventions

- `id UUID PK DEFAULT gen_random_uuid()`; `created_at`/`updated_at timestamptz` (append-only tables have `created_at` only).
- The application role has **no UPDATE/DELETE grants** on `ledger_entries`, `fills`, `order_events` (plus a belt-and-braces trigger). Append-only is structural.
- **Numeric types:** money `NUMERIC(18,2)`; prices `NUMERIC(18,4)`; quantities `BIGINT` (whole shares); position cost-basis `NUMERIC(20,4)`. Drizzle maps NUMERIC to strings → Decimal; **never JS number**.
- No DB trigger reproduces the order-transition table — the domain machine owns transitions; the DB keeps structural constraints only.

## Tables

| Table                        | Responsibility            | Key fields & constraints                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| users / sessions / auth_*    | Better Auth identity      | email UNIQUE; scrypt hashes                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| accounts                     | Arthosrot trading account | user_id FK; mode CHECK ('PAPER'); status enum PROVISIONING/ACTIVE/ARCHIVED/PROVISIONING_FAILED; **partial unique index: one ACTIVE per user**; currency ('USD'); starting_cash NUMERIC(18,2); cash_balance NUMERIC(18,2) — cached projection, **no ≥0 CHECK** (a real fill must never be rejected by the projection; drift is a reconciliation alarm)                                                                                                                            |
| broker_accounts              | Venue account mapping     | trading_account_id FK; broker enum; external_account_id, UNIQUE(broker, external_account_id); status; last_reconciled_at; last_stream_event_at; reconciliation_status enum HEALTHY/STALE/RECONCILING/DRIFT_DETECTED/ERROR. **No credentials here** — venue creds are firm-level env secrets                                                                                                                                                                                      |
| stream_cursors               | SSE resume cursors        | UNIQUE(broker, stream); last_ulid; updated_at                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| instruments                  | Reference data            | symbol UNIQUE (uppercase CHECK); name; exchange; status                                                                                                                                                                                                                                                                                                                                                                                                                          |
| orders                       | Order lifecycle           | account_id, instrument_id FKs; side/type/tif enums; qty BIGINT CHECK > 0; limit_price NUMERIC(18,4) CHECK (limit_price IS NULL) = (type='MARKET'); state enum (EXECUTION.md); filled_qty BIGINT CHECK 0 ≤ filled_qty ≤ qty; reject_reason; needs_attention bool; idempotency_key, UNIQUE(account_id, idempotency_key); broker; broker_order_id, UNIQUE(broker, broker_order_id) nulls distinct; client_order_id = id. Indexes: (account_id, state); partial index on open states |
| order_events                 | Canonical audit           | order_id FK; canonical_event_type; from_state/to_state; source enum broker/local/inferred/reconciliation; broker; external_event_id, UNIQUE(broker, external_event_id) nulls distinct; occurred_at; received_at; raw_payload jsonb (redacted). Append-only                                                                                                                                                                                                                       |
| fills                        | Execution facts           | order_id FK; qty BIGINT CHECK > 0; price NUMERIC(18,4); fee NUMERIC(18,2); notional NUMERIC(18,2); execution_id, UNIQUE(broker, execution_id); occurred_at. Append-only                                                                                                                                                                                                                                                                                                          |
| positions                    | Holdings                  | UNIQUE(account_id, instrument_id); qty BIGINT CHECK ≥ 0; cost_basis_total NUMERIC(20,4) CHECK ≥ 0                                                                                                                                                                                                                                                                                                                                                                                |
| ledger_entries               | Authoritative cash record | account_id FK; entry_type enum DEPOSIT/TRADE/FEE/ADJUSTMENT (reserved: WITHDRAWAL/DIVIDEND/CORPORATE_ACTION/RECONCILIATION_ADJUSTMENT); amount NUMERIC(18,2) CHECK ≠ 0 (signed); ref_type/ref_id; description. Append-only; index (account_id, created_at DESC)                                                                                                                                                                                                                  |
| watchlists / watchlist_items | Watchlists                | UNIQUE(watchlist_id, instrument_id); sort_order                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| market_data_cache            | Provider cache            | UNIQUE(cache_key); payload jsonb; fetched_at; stale_after                                                                                                                                                                                                                                                                                                                                                                                                                        |

## Reset flow

Cancel eligible open orders at the venue (await terminal events or timeout → reconcile) → set account + broker_account ARCHIVED → create new account (PROVISIONING) → `provisionAccount()` → fund → DEPOSIT entry → ACTIVE. History remains queryable under the archived account. Nothing is deleted.

## Migrations

drizzle-kit generates SQL into `drizzle/` (committed, sequential, immutable once merged). Local: `pnpm db:migrate`. CI applies all migrations from zero on every run (validation). Production migrations are an explicit approval-gated release step — never automatic on cold start. Philosophy: roll forward; destructive operations (DROP, type narrowing, data rewrites) require a `-- destructive: <justification>` header, human sign-off, and a backup (pg_dump or Neon branch) first.
